services:
  web:
    build: .
    ports:
      - "${PORT:-3001}:${PORT:-3001}"
    env_file:
      - .env.local
    environment:
      - PORT=${PORT:-3001}
      - DATABASE_URL=file:/app/data/dev.db
    volumes:
      - mtime-data:/app/data

volumes:
  mtime-data:
